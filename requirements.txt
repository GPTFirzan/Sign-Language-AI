streamlit>=1.32.0
streamlit-webrtc>=0.47.0
av>=10.0.0
opencv-python-headless>=4.7.0.68
# Dikunci ke versi yang masih punya API mediapipe.solutions (dipakai app ini).
# Versi 0.10.30 ke atas MENGHAPUS mediapipe.solutions sama sekali -> app akan
# gagal total kalau versi tidak dikunci di sini.
mediapipe==0.10.14
# Dikunci PERSIS sama dengan versi yang dipakai saat model.p dilatih ulang.
# scikit-learn terbaru mengubah struktur internal pohon (DecisionTreeClassifier),
# jadi model.p versi lama akan CRASH dengan error "no attribute 'monotonic_cst'"
# begitu predict_proba() dipanggil (yaitu tepat saat tangan terdeteksi) kalau
# versinya tidak cocok dengan versi training.
scikit-learn==1.8.0
Pillow>=9.0.0
numpy>=1.21.0
requests>=2.28.0
